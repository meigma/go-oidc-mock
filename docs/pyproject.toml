[project]
name = "go-oidc-mock-docs"
version = "0.0.0"
description = "MkDocs documentation site for go-oidc-mock."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
    "neoteroi-mkdocs==1.2.0",
]

[tool.uv]
package = false
